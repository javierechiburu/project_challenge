1.- Para el desarrollo del proyecto se utiliza PHP con el framework Symfony:
	-Crear de proyecto: symfony new my_project_name --full

2.- Instalar dependencias a través de composer:

	-PHPUnited: crear archivo phpunit.xml (en este caso ya venia instalado en la creacion del proyecto)
	-phplint: composer require overtrue/phplint:^3.0 --dev -vvv
	-phpstan: composer require --dev phpstan/phpstan

3.- Crear controlador .
4.- Crear en carpeta test archivo "ChallengeTest.php" para pruebas.
